import 'package:flutter/material.dart';

class SiftScreen extends StatelessWidget {
  const SiftScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sift Screen'),
    );
  }
}